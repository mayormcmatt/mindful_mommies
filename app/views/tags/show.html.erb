<h3>Articles tagged with <%= @tag.name %></h3>

<ul>
  <% @tag.articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %>
    </li>
  <% end %>
</ul>

<%= link_to "< Back to Front Page", articles_path %>